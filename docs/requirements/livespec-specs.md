# livespec — Spec catalog

Canonical authoring is **OpenSpec** under [`openspec/specs/`](../../openspec/specs/).

```
index_project(workspace="/abs/path/to/livespec-mcp")
sync_openspec(openspec_dir="openspec")
```

Optional link seed (slug ids): [`livespec-spec-links.json`](./livespec-spec-links.json).

The former native `## SPEC-NNN:` catalog in this file is removed.
