# Livespec session — FastAPI

Copy into a new Cursor chat (replace `WORKSPACE`):

```
FastAPI repo. Livespec workspace:

  WORKSPACE

Use livespec MCP with workspace= on every call.

Goals this session:
1. Confirm index + RF Explorer at /explorer
2. List HTTP routes (GET/POST) via find_endpoints(framework="fastapi")
3. [YOUR TASK HERE]

Start: get_project_overview → index_project(explorer=True) if needed → export_explorer → tell me the /explorer URL after uvicorn.
```

Installed by `livespec-mcp fastapi init` — edit `WORKSPACE` and the task line.
